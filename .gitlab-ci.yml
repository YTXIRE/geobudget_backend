stages:
  - build
  - docker
  - deploy

variables:
  DOCKER_TLS_CERTDIR: ""
  REGISTRY: "localhost:5000"
  IMAGE_NAME: "$REGISTRY/geobudget-backend"

build:
  stage: build
  tags:
    - docker
  image: gradle:8.10-jdk24
  cache:
    key: gradle-cache
    paths:
      - .gradle/wrapper
      - .gradle/caches
  script:
    - echo "===== 🧹 START BUILD STAGE ====="
    - echo "👉 Проверяем версию Gradle..."
    - gradle --version
    - echo "👉 Чистим проект и собираем bootJar..."
    - gradle clean bootJar --stacktrace
    - echo "✅ Сборка завершена, jar создан"
    - echo "===== 🧹 END BUILD STAGE ====="
  artifacts:
    paths:
      - build/libs/*.jar

docker_build_push:
  stage: docker
  tags:
    - docker
  image: docker:25.0.3
  services:
    - docker:25.0.3-dind
  script:
    - echo "===== 🐳 START DOCKER BUILD & PUSH STAGE ====="
    - echo "👉 Проверяем доступность Docker..."
    - docker info
    - echo "👉 Логинимся в локальный registry (может пропустить ошибку)..."
    - docker login $REGISTRY || echo "⚠️ Не удалось залогиниться"
    - echo "👉 Собираем Docker-образ:"
    - docker build -t $IMAGE_NAME:latest .
    - echo "👉 Пушим образ в $REGISTRY"
    - docker push $IMAGE_NAME:latest
    - echo "✅ Docker image собран и отправлен"
    - echo "===== 🐳 END DOCKER BUILD & PUSH STAGE ====="

deploy:
  stage: deploy
  tags:
    - docker
  image: docker:25.0.3
  services:
    - docker:25.0.3-dind
  script:
    - echo "===== 🚀 START DEPLOY STAGE ====="
    - echo "👉 Проверяем доступность Docker..."
    - docker info
    - echo "👉 Тянем образ $IMAGE_NAME:latest"
    - docker pull $IMAGE_NAME:latest
    - echo "👉 Переходим в директорию проекта:"
    - cd $CI_PROJECT_DIR
    - echo "👉 Останавливаем предыдущие контейнеры..."
    - docker compose down || echo "⚠️ Не было контейнеров для остановки"
    - echo "👉 Запускаем новые контейнеры..."
    - docker compose up -d
    - echo "✅ Деплой завершён"
    - echo "===== 🚀 END DEPLOY STAGE ====="
